services:
  - type: web
    name: product-explorer-showcase
    runtime: static
    buildCommand: echo Static site ready
    staticPublishPath: render-site
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
